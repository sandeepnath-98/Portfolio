# Example environment file
# Copy this to `.env` and edit values for local development

# Admin password for the messages/admin area
ADMIN_PASSWORD=admin123

# Port the app should listen on
PORT=5000
 
# MongoDB connection URL (optional). Example for local docker-compose below:
# MONGODB_URL=mongodb://localhost:27017/devfolio
